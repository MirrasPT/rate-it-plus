# Ficheiros de dependências do Node.js
node_modules/

# Ficheiros de ambiente (devem ser mantidos privados)
.env

# Ficheiros do macOS
.DS_Store

# Ficheiros de base de dados local (se não quiser partilhá-la diretamente no Git)
# Se quiser que a base de dados seja parte do repositório, comente a linha abaixo
rateitplus_local.db
*.sqlite
*.sqlite3

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Ficheiros de build e output de testes
build/
dist/
coverage/